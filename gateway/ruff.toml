# Ruff linter configuration for ForgeDash Gateway
# See: https://docs.astral.sh/ruff/

target-version = "py312"

[lint]
select = ["F", "E", "W", "I"]
ignore = [
  "E501",  # line-too-long — keep relaxed for readability
]
